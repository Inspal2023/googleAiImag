# 香蕉AI图片工作室 (Banana AI Image Studio)

![License](https://img.shields.io/badge/license-MIT-blue.svg) ![React](https://img.shields.io/badge/React-19.0-61DAFB.svg) ![Gemini](https://img.shields.io/badge/AI-Gemini_2.5_%26_3.0-8E75B2.svg)

**一个由 Google Gemini 驱动的新一代产品图片创意转换工具，采用现代化的液态玻璃（Liquid Glass）UI 设计。**

## 📖 项目简介

在电商运营、工业设计和数字营销领域，快速获取高质量的产品变体图是一项耗时且昂贵的工作。传统的3D渲染或人工修图往往无法满足快速迭代的需求。

**香蕉AI图片工作室** 旨在解决这一痛点。它利用最新的 Google Gemini 多模态模型能力，让用户仅需上传一张产品原图，即可在几秒钟内生成工程线稿、标准三视图、或是融合了新材质与背景的营销大片。

本项目特别适合**产品设计师**、**电商卖家**以及**创意总监**使用，帮助他们在构思阶段快速验证创意，或以极低的成本生成高质量的视觉素材。

## ✨ 主要功能

*   **🎨 智能线稿生成 (Line Art)**
    *   将产品照片一键转换为专业的黑白线稿。支持**工程制图**（强调结构准确性）和**概念草图**（强调设计感）两种模式，并可精细调节细节程度和线条风格。
*   **📐 多角度视图合成 (Multi-View)**
    *   基于单张图片生成产品的空间视图。提供标准的**工业三视图**（正交投影），以及支持自定义俯仰角（Pitch）和旋转角（Yaw）的**自由视角**模式，精准控制相机透视。
*   **🌌 场景与特征融合 (Scene Fusion)**
    *   超越简单的背景替换。不仅可以通过文字描述生成背景，更支持**混合模式（Hybrid Mode）**，允许上传参考图，将参考对象的**结构、颜色、材质**特征以可控的权重融合到原产品中。
*   **✨ 智能提示词优化**
    *   内置基于 `gemini-3-pro-preview` 的提示词优化引擎。用户只需输入简单的关键词，AI 即可将其扩展为包含光影、氛围和镜头语言的专业级提示词。

## 🛠️ 技术栈

本项目采用纯前端架构，结合了最新的 React 特性和 Google GenAI SDK。

*   **核心框架**: [React 19](https://react.dev/)
*   **开发语言**: [TypeScript](https://www.typescriptlang.org/)
*   **AI SDK**: [@google/genai](https://www.npmjs.com/package/@google/genai) (Google Gemini API)
*   **样式方案**: [Tailwind CSS](https://tailwindcss.com/) (配合自定义的 Glassmorphism 样式)
*   **构建工具**: [Vite](https://vitejs.dev/)
*   **字体**: Inter (Google Fonts)

## 🚀 安装与启动指南

请按照以下步骤在本地环境运行本项目。

### 环境要求
*   Node.js >= 18.0.0
*   npm 或 yarn
*   有效的 Google Gemini API Key

### 1. 克隆项目
```bash
git clone https://github.com/your-username/banana-ai-studio.git
cd banana-ai-studio
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置环境变量
在项目根目录创建 `.env` 文件（如果没有），并添加您的 API Key：
```env
# 注意：在生产环境中，请勿将 Key 暴露在前端代码中
API_KEY=your_google_gemini_api_key_here
```
*注意：本项目演示代码使用了 `process.env.API_KEY`，请确保您的构建工具（如 Vite）已正确配置以替换此变量。*

### 4. 启动本地开发服务器
```bash
npm run dev
```
启动后，访问终端输出的本地地址（通常为 `http://localhost:5173`）即可使用。

## 📂 项目结构

```text
banana-ai-studio/
├── index.html              # 入口 HTML，包含 Tailwind CDN 和字体
├── index.tsx               # React 应用挂载点
├── App.tsx                 # 主应用组件，包含状态管理和核心布局
├── types.ts                # TypeScript 类型定义（Feature, Options等）
├── constants.ts            # 常量配置，包含提示词模板和 UI 文本
├── services/
│   └── geminiService.ts    # 核心服务：封装 Google GenAI API 调用逻辑
├── components/             # UI 组件库
│   ├── Header.tsx          # 顶部导航栏
│   ├── FeatureSelector.tsx # 功能选择卡片
│   ├── ImageUploader.tsx   # 图片上传与拖拽组件
│   ├── PromptInput.tsx     # 提示词输入与优化组件
│   ├── ResultDisplay.tsx   # 结果展示与下载区域
│   ├── LineArtSettings.tsx # 线稿参数设置面板
│   ├── MultiViewSettings.tsx # 多视图参数设置面板
│   └── ProductFusionSettings.tsx # 融合模式滑块组件
└── ...
```

## 🤝 贡献
欢迎提交 Issue 或 Pull Request 来改进本项目！

## 📄 许可证
[MIT](LICENSE)
